1. Aim
To implement Support Vector Machine (SVM) classification using Python.
2. Theory
Support Vector Machine (SVM) is a supervised machine learning algorithm used for classification and prediction.
It separates data using a decision boundary called hyperplane.

(Write only 2–3 lines in exam)

3. Requirements
1. Python
2. NumPy
3. Matplotlib
4. Scikit-learn
4. Installation Command

Open Command Prompt and type:

pip install numpy matplotlib scikit-learn
5. Step-by-Step Procedure
STEP 1: Import Required Libraries
Code
import numpy as np

import matplotlib.pyplot as plt

from sklearn import datasets

from sklearn.model_selection import train_test_split

from sklearn.svm import SVC

from sklearn import metrics
Explanation
Imports required libraries for dataset handling, model creation, prediction and visualization.
STEP 2: Load Dataset
Code
iris = datasets.load_iris()
Explanation
Loads Iris flower dataset.
STEP 3: Select Features and Target
Code
X = iris.data[:, :2]

y = iris.target
Explanation
Selects first two features for visualization and target class labels.
STEP 4: Split Dataset
Code
X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=0.3, random_state=1
)
Explanation
Splits dataset into training and testing data.
70% data is used for training and 30% for testing.
STEP 5: Create SVM Model
Code
model = SVC(kernel='linear')
Explanation
Creates SVM classifier using linear kernel.
STEP 6: Train Model
Code
model.fit(X_train, y_train)
Explanation
Trains the SVM model using training data.
STEP 7: Make Predictions
Code
y_pred = model.predict(X_test)
Explanation
Predicts output for testing data.
STEP 8: Print Accuracy
Code
print("Accuracy:", metrics.accuracy_score(y_test, y_pred))
Explanation
Calculates accuracy of SVM model.
Accuracy Formula

Accuracy=
Total Predictions
Correct Predictions
	​


STEP 9: Predict New Sample
Code
sample = [[5.1, 3.5]]

print("Prediction for sample:", model.predict(sample))
Explanation
Predicts class for new sample data.
STEP 10: Create Mesh Grid
Code
h = .02

x_min, x_max = X[:, 0].min() - 1, X[:, 0].max() + 1

y_min, y_max = X[:, 1].min() - 1, X[:, 1].max() + 1

xx, yy = np.meshgrid(
    np.arange(x_min, x_max, h),
    np.arange(y_min, y_max, h)
)
Explanation
Creates mesh grid for plotting decision boundary.
STEP 11: Predict for Grid
Code
Z = model.predict(np.c_[xx.ravel(), yy.ravel()])

Z = Z.reshape(xx.shape)
Explanation
Predicts output for all grid points.
STEP 12: Plot Decision Boundary
Code
plt.figure(figsize=(10,6))

plt.contourf(xx, yy, Z, alpha=0.4)

plt.scatter(X[:, 0], X[:, 1], c=y, edgecolors='k')

plt.xlabel("Sepal Length")

plt.ylabel("Sepal Width")

plt.title("SVM Decision Boundary")

plt.show()
Explanation
Displays graphical decision boundary of SVM classifier.
6. Complete Program
# Import libraries

import numpy as np
import matplotlib.pyplot as plt

from sklearn import datasets
from sklearn.model_selection import train_test_split
from sklearn.svm import SVC
from sklearn import metrics

# Load dataset

iris = datasets.load_iris()

# Take only 2 features

X = iris.data[:, :2]
y = iris.target

# Split dataset

X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=0.3, random_state=1
)

# Create SVM model

model = SVC(kernel='linear')

# Train model

model.fit(X_train, y_train)

# Prediction

y_pred = model.predict(X_test)

# Accuracy

print("Accuracy:", metrics.accuracy_score(y_test, y_pred))

# Sample prediction

sample = [[5.1, 3.5]]

print("Prediction for sample:", model.predict(sample))

# Plot Decision Boundary

h = .02

x_min, x_max = X[:, 0].min() - 1, X[:, 0].max() + 1

y_min, y_max = X[:, 1].min() - 1, X[:, 1].max() + 1

xx, yy = np.meshgrid(
    np.arange(x_min, x_max, h),
    np.arange(y_min, y_max, h)
)

Z = model.predict(np.c_[xx.ravel(), yy.ravel()])

Z = Z.reshape(xx.shape)

plt.figure(figsize=(10,6))

plt.contourf(xx, yy, Z, alpha=0.4)

plt.scatter(X[:, 0], X[:, 1], c=y, edgecolors='k')

plt.xlabel("Sepal Length")

plt.ylabel("Sepal Width")

plt.title("SVM Decision Boundary")

plt.show()
7. Output
Accuracy
Accuracy: 0.97
Prediction
Prediction for sample: [0]
Graph
SVM Decision Boundary graph is displayed.
8. Result
SVM classification model was implemented successfully and accurate predictions were obtained.
9. Conclusion
Support Vector Machine algorithm was successfully used for classification using Iris dataset.